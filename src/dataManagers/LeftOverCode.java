package dataManagers;

class LeftOverCode {

    //Previous move Head
    {
       /* public static void moveHead() {
            int hX = Body.bodies.get(0).getX();
            int hY = Body.bodies.get(0).getY();

            Body snakeHead = Body.bodies.get(0);
            Direction hD = snakeHead.getDirection();
            // GamePanel.gridCO[hX][hY] = null;
            followAhead(hX, hY, hD);

            switch (hD) {
                case UP -> {
                    if(hY == 0) {
                        hY = GamePanel.num_of_tiles;
                        Body.bodies.get(0).setY(hY);
                    }else {
                        // GamePanel.gridCO[hX][hY - 1] = snakeHead;
                        Body.bodies.get(0).setY(hY - 1);
                    }
                }
                case DOWN -> {
                    if (hY == (GamePanel.num_of_tiles - 1)) {
                        hY = -1;
                        Body.bodies.get(0).setY(hY);
                    } else {
                        //GamePanel.gridCO[hX][hY + 1] = snakeHead;
                        Body.bodies.get(0).setY(hY + 1);
                    }
                }
                case LEFT ->{
                    if(hX == 0) {
                        hX = GamePanel.num_of_tiles;
                        Body.bodies.get(0).setX(hX);
                    }else {
                        // GamePanel.gridCO[hX - 1][hY] = snakeHead;
                        Body.bodies.get(0).setX(hX - 1);
                    }
                }
                case RIGHT ->{
                    if(hX == (GamePanel.num_of_tiles - 1)) {
                        hX =-1;
                        Body.bodies.get(0).setX(hX);
                    }else {
                        //GamePanel.gridCO[hX + 1][hY] = snakeHead;
                        Body.bodies.get(0).setX(hX + 1);
                    }
                }
            }
    //        Body.bodies.get(0).setX(getXIndexOf(Type.HEAD));
    //        Body.bodies.get(0).setY(getYIndexOf(Type.HEAD));
        }*/
    }


}
